[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "voigtinference"
version = "1.0.0"
description = "Exact likelihood calculus and conditional attribution for the Voigt profile"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "Peter Reinhard Hansen", email = "hansen@unc.edu" },
    { name = "Chen Tong" },
]
keywords = [
    "Voigt profile",
    "Faddeeva function",
    "maximum likelihood",
    "Fisher information",
    "line shape",
    "deconvolution",
    "spectroscopy",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
]

[project.optional-dependencies]
test = ["pytest>=7", "mpmath>=1.3"]
examples = ["matplotlib>=3.6", "iminuit>=2.21", "lmfit>=1.3"]

[project.urls]
Homepage = "https://github.com/reinhardhansen/voigtinference"
Repository = "https://github.com/reinhardhansen/voigtinference"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
